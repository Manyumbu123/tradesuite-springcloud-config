# Application YAML
server:
  servlet:
    context-path: /v2/api
  error:
    include-exception: false

logging:
  level:
    za.co.standardbank.tradesuiteservice: DEBUG
    org.springframework.web.filter.CommonsRequestLoggingFilter: D§EBUG
    org.liquibase: DEBUG
    '[org.hibernate.sql]': DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
